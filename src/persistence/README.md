# persistence/

Guardado/carga del estado (localStorage) y migraciones de esquema.

Regla: puede importar tipos de `../core/`, pero `../core/` nunca importa de aquí. Se implementa a
partir de MVP-4 (ver `docs/plan-maestro.md`).
